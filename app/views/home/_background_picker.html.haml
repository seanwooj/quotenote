%ul.backgrounds.cf
  - @backgrounds.each do |bg|
    %li.bg{:id => "background-#{bg.id}", :style => "background-image: url(#{bg.image.url(:small)})", :data => {:id => bg.id, :repeating => bg.repeating.to_s}}

