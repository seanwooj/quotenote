!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title Quotenote
    = render :partial => 'generator/image_styles'
    = render :partial => "generator/fonts/#{@options[:font_family]}"
    = csrf_meta_tags
  %body

    #quotenote{:class => "#{'cover-background' unless @options[:repeating] == 'true'}"}
      .overlay{:class => "#{'active' if @options[:overlay] == 'true' || @options[:overlay] == true}"}
      .quote.cf
        .quote-text
          - @options[:quote_text].each do |line|
            .quote-text-line
              = line
        .author-text
          = @options[:quote_author]

  :javascript
    $(window).load(function(){
      window.qn.generator.init();
    });
